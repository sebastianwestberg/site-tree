default:
    suites:
        config:
            contexts:
                - ConfigContext
            paths: [%paths.base%/features/config]
        formatter:
            contexts:
                - FormatterContext
            paths: [%paths.base%/features/formatter]
